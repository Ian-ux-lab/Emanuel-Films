[variables]
NIXPACKS_PHP_VERSION = '8.2'

[phases.setup]
nixPkgs = ['php82', 'php82Extensions.pdo_sqlite', 'composer']

[phases.build]
cmds = ['composer install --no-dev --no-interaction --optimize-autoloader']

[start]
cmd = 'php artisan serve --host=0.0.0.0 --port=$PORT'
